	
   <%= link_to '', logo_destroy_companies_path(logo.imageable_id, logo.id), method: :delete, data: { confirm: 'Are you sure?' }, :remote => true, class: 'destroy-img' %>
	<%= image_tag(logo.image.remote_url, class: 'img-thumbnail custom-brand-img') %>

