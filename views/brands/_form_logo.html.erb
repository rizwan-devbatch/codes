
  <div id="brand-logo-form-container" class="file-input-logo" <%= 'style=display:none' if logo %>>
    <%= simple_form_for Image.new, :url => logo_brands_path, html: {:id => "brand-logo-form"} do |f| %>
      <%= hidden_field_tag :id, @brand.id %>
      <div class="form-group">
           <%= f.file_field :image, required: true, multiple: true, name: 'image[image]', class: 'logo-file-input' %>
          <input type="button" class="file_upload_btn w94px" value="Upload Logo" >
    
      </div>
      <div class="progress-wrapper">
        <p>Bitrate: <span class="bitrate"></span></p>
        <div class="progress">
          <div class="progress-bar" role="progressbar">
            0%
          </div>
        </div>
      </div>
    <% end %>
  </div>
 