<% if @image.new_record? %>
alert('The photo could not be uploaded: <%= j @image.errors.full_messages.join(', ').html_safe %>');
<% else %>
$('#brand-logo').prepend('<%= j render @image %>');
<% end %>